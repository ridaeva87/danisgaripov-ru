
## 1. Кнопка «Получить финансовый разбор» в услугах
- На страницах услуг (`ServicePage.tsx`) кнопка «Получить финансовый разбор» сейчас ведёт на `/#lead-form`. Изменить на ссылку к блоку мини-опроса на главной — добавить id `quiz` секции мини-опроса в `Index.tsx` и перевести кнопку на `/#quiz`.
- Аналогично у кнопок «Получить финансовый разбор» новой услуги IT-разработка.

## 2. Главная — секция «Бесплатный финансовый разбор»
- Удалить блок `LeadForm` под мини-опросом (`#financial-analysis-form`, lines 335–344 в `Index.tsx`) — заголовок «Получить финансовый разбор».

## 3. Секция «Контакты и форма»
В `Index.tsx` (lines 534–553) заменить список контактов на 3 пункта с иконками:
- Telegram-канал Даниса Гарипова → https://t.me/garipovdanis (иконка Send)
- Канал в MAX (если Telegram недоступен) — без ссылки, пометка «Скоро» (ждём ссылку)
- Связаться с менеджером → https://t.me/Albina_assistent (иконка MessageCircle)

Каждый пункт — кликабельная карточка (target=`_blank`).

## 4. Услуга «Срочный выкуп авто»
В `src/data/services.ts`:
- Убрать `comingSoon: true`.
- Заменить `heroDescription`, `intro`, `suitableFor`, `steps` и добавить `whyUs` согласно тексту из ТЗ.
- На странице услуги (`ServicePage.tsx`) условный блок «СКОРО» уберётся автоматически.
- На главной карточке услуги бэйдж «СКОРО» больше не показывается.

## 5. Новая услуга «IT-разработка для бизнеса»
- Сохранить присланное изображение через `lovable-assets create` (из `/mnt/user-uploads/`), получить asset.json в `src/assets/it-development.jpg.asset.json`.
- Добавить новый элемент в `services` (`src/data/services.ts`): slug `it-razrabotka`, иконка из lucide (например `Cpu`), `shortDescription`, `heroDescription`, `intro`, `suitableFor`, `steps`, `whyUs`, `bonus` (раздел «Что вы получаете») по тексту из ТЗ.
- В `ServicePage.tsx` использовать кастомное изображение, если оно указано в данных услуги: добавить опциональное поле `heroImage?: string` в `ServiceItem`. Если поле есть — показывать его вместо `danisHero`.
- На hero услуги остаются кнопки «Оставить заявку» и «Получить финансовый разбор» — переведённая на `/#quiz` (см. п.1).

## 6. Блок «Почему мне доверяют»
В `Index.tsx` удалить массив `trustSignals` (lines 54–70) и саму сетку карточек (lines 355–365). Оставить только заголовок и подзаголовок секции.

## 7. Блок «Гарипов финансы»
В `Index.tsx` (lines 369–433):
- Удалить колонку с фото (`danisPortrait`, lines 421–429), оставить только левый блок, расширить на всю ширину контейнера (убрать `lg:grid-cols-[1.1fr_1fr]`).
- Изменить подписи статов:
  - `проведённых сделок` → две строки `ПРОВЕДЁННЫХ` / `СДЕЛОК`
  - `сопровождение` → одна строка `СОПРОВОЖДЕНИЕ`
  - `по сути, без шума` → `ПО ВАШЕМУ ЗАПРОСУ`
  - `направлений сервиса` → две строки `НАПРАВЛЕНИЙ` / `СЕРВИСА`
- Реализация многострочности — через массив строк в данных, рендерится через `<br />` или `flex flex-col`.
- Удалить импорт `danisPortrait`.

## 8. Блок «Кому подходит»
В `Index.tsx` массив `audience` (lines 33–39) добавить пункт: `«если вам нужно автоматизировать бизнес и масштабироваться»`.

## 9. Блок «Благотворительность»
В `Index.tsx` (lines 487–498):
- Кнопка «Участвовать / внести вклад» — заменить `<a href="#lead-form">` на `<button type="button">` без действия (или onClick noop), сохранить стиль.
- Удалить кнопку «Связаться по разделу».

## Технические детали
- Файлы: `src/pages/Index.tsx`, `src/pages/ServicePage.tsx`, `src/data/services.ts`, новый `src/assets/it-development.jpg.asset.json`.
- Загрузка картинки: `lovable-assets create --file /mnt/user-uploads/ChatGPT_Image_6_июня_2026_г._13_33_54.png --filename it-development.jpg > src/assets/it-development.jpg.asset.json`.
- Новых зависимостей, миграций или backend-кода нет.
