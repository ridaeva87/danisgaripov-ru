**Что сделаю**

1. **Починю съезжающий текст в карточках статистики (блок «ГАРИПОВ финансы»)**
   - В мобильной версии длинные подписи («проведённых сделок», «сопровождение», «направлений сервиса», «разбор ситуации») вылезают за края карточек, потому что у них стоит `uppercase` + `tracking-wider` и слова не переносятся.
   - Уменьшу размер шрифта подписи на мобильном (примерно `text-[9px]`), уберу `tracking-wider` на мобильном (оставлю только на `sm:`), добавлю `break-words` / `hyphens-auto` и уменьшу внутренние отступы карточек на мобильном, чтобы текст всегда помещался внутри карточки.
   - При необходимости уменьшу также размер цифр (`text-lg` на мобильном вместо `text-xl`), чтобы карточки выглядели сбалансированно.
   - На планшете/десктопе (`sm:` и выше) внешний вид карточек оставлю как сейчас.

2. **Сделаю фото Даниса в нижнем блоке крупнее и без обрезки ног**
   - В блоке справа от статистики (`danisPortrait`) увеличу высоту изображения: на мобильном с `h-[480px]` примерно до `h-[560px]` (а высоту контейнера дам расти под фото), на `sm:` с `h-[600px]` до `h-[680px]`, на `lg:` с `h-[720px]` до `h-[820px]`.
   - Поменяю `object-bottom` на `object-contain object-center` (или `object-top`), чтобы фигура целиком влезала в кадр и ноги не подрезались.
   - Уберу любые ограничения по высоте у родителя, если они мешают фото показаться полностью, и при необходимости добавлю `max-h-none`.

3. **Контент и стиль не трогаю**
   - Тексты, цифры, цвета, кнопки и структуру блока оставляю без изменений.
   - Меняю только мобильную типографику карточек статистики и размеры/позицию фото Даниса в этом блоке.
   - Hero-секцию в этот раз не трогаю — правка только в нижнем светлом блоке и в карточках статистики.

**Технически**

- Изменения только в `src/pages/Index.tsx`, строки ~409–447 (карточки статистики и блок с `danisPortrait`).
- Никакие ассеты не заменяю, фото Даниса остаётся то же самое (`src/assets/danis-garipov-portrait.png`).
