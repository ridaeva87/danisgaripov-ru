# План правок

## 1. Главная страница (`src/pages/Index.tsx`)
- В блоке «Подход» (стр. ~206) заменить фразу на: «Решения под конкретную задачу, сумму и срок.»
- Соцсети в блоке «Бесплатный финансовый разбор» (стр. ~343–353): заменить ссылки — Telegram → `https://t.me/garipovdanis`, Instagram → `https://www.instagram.com/danisgaripov?igsh=MXZweTQ2MnNzcGk3cg==`. Кнопку YouTube убрать (нет ссылки).
- Кнопку WAITLIST убрать из карточек услуг (стр. ~277–279). Вместо неё после успешной отправки формы заявки показывать всплывающую красную кнопку/тост «WAITLIST — вы в очереди» (реализуем через состояние в `LeadForm` + красный toast, либо модальный бейдж).

## 2. Услуги (`src/data/services.ts`)
- Полностью удалить услугу `kreditnaya-kultura` (Кредитная культура).
- Услугу `srochnyy-vykup-avto` пометить как `comingSoon: true` — в карточке на главной рядом с названием уже отображается «скоро», но по запросу сделаем крупнее и красным акцентом.
- Переписать `heroDescription`, `suitableFor`, `steps`, `trustPoints`, добавить новые поля `whyUs` (Почему мы / Что вы получаете) и `differences` (для «Частных займов» — отличия от банка, «что повышает шансы») для следующих услуг согласно присланным текстам:
  - `vozvrat-strahovok` (Возврат страховок) — добавить интро, «Кому подходит», «Почему мы», «Как мы работаем».
  - `kreditovanie` (Кредитование) — интро + «Кому подходит» + «Как мы работаем».
  - `uluchshenie-kreditnoy-istorii` → переименовать в «Диагностика кредитной истории», обновить тексты «Кому подходит» (галочки) + «Как мы работаем».
  - `chastnye-zaymy` (Частные займы) — интро + «Чем отличается от банковского кредита» + «Кому подходит» + «Что повышает шансы».

## 3. Страница услуги (`src/pages/ServicePage.tsx`)
- Удалить секцию «Короткий блок доверия» полностью.
- Добавить рендер новых секций по доступным полям: «Почему мы / Что вы получаете», «Чем отличается от банковского кредита», «Что повышает шансы».
- Для услуги «Срочный выкуп авто» (comingSoon) скрыть форму и шаги, показать крупный бейдж «СКОРО».
- В форме заявки (`LeadForm`) использовать новый чекбокс согласия (см. п. 5).

## 4. Документы внизу сайта
- Скопировать загруженные `.docx` файлы из `user-uploads://` в `public/docs/` с короткими именами:
  - `politika-personalnyh-dannyh.docx`
  - `soglasie-personalnyh-dannyh.docx`
  - `soglasie-na-reklamu.docx`
- В `src/pages/Index.tsx` добавить новую секцию в самом конце (перед `</main>`): три кнопки, открывающие файлы в новой вкладке (`<a href="/docs/...docx" target="_blank">`). Кнопки оформить в стиле сайта (`variant="soft"`).

## 5. Согласие на обработку ПД в форме (`src/components/LeadForm.tsx`)
- Добавить `Checkbox` + `Label` со ссылкой на `/docs/politika-personalnyh-dannyh.docx` — «Согласен на обработку персональных данных».
- Кнопка submit становится disabled пока галочка не отмечена. Без галочки toast не показываем.
- После успешной отправки показывать красную плашку/toast «WAITLIST — вы в очереди» (см. п. 1).

## Технические детали
- `services.ts`: расширить тип `ServiceItem` опциональными полями `intro?: string`, `whyUs?: { title: string; items: string[] }`, `differences?: { title: string; items: string[] }`, `bonusItems?: { title: string; items: string[] }`. `suitableFor` остаётся массивом строк (поддержим эмодзи как есть).
- `ServicePage.tsx`: рендерить новые блоки условно (`service.whyUs && ...`).
- Удаление `WaitlistButton` из карточек — компонент можно удалить или переиспользовать как pop-up после submit.
- Чекбокс согласия: используем существующие `@/components/ui/checkbox` и `@/components/ui/label`.

## Файлы, которые будут затронуты
- `src/data/services.ts` — переписать данные, убрать «Кредитную культуру».
- `src/pages/Index.tsx` — фраза «Подход», соцсети, удаление WaitlistButton, секция документов.
- `src/pages/ServicePage.tsx` — убрать «короткий блок доверия», добавить новые секции, спец-обработка comingSoon.
- `src/components/LeadForm.tsx` — чекбокс согласия + waitlist-уведомление после submit.
- `public/docs/*.docx` — три новых файла из загрузок.

После реализации проверю верстку на мобильной ширине (390px).